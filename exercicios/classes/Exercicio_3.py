# Crie uma classe chamada Usuario que receberá os seguintes atributos no construtor:
# id, username, password, logado: boolean
# e terá os seguintes métodos:
# login: recebe um username e um password e retorna a mensagem "Usuário logado" se o username e o password forem iguais aos atributos da classe, caso contrário retorna a mensagem "Usuário ou senha incorretos".
# Caso logado mude o atributo logado para True, caso contrário mude o atributo logado para False.
# logout: retorna a mensagem "Usuário deslogado".

class Usuario:
    def __init__(self):
        pass

    def login(self, username, password):
        pass

    def logout(self):
        pass

# Faça um teste com a classe criada e verifique se os métodos estão funcionando corretamente.